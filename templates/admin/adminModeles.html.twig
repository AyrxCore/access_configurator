<div class="tab-pane active" id="tab_1">
    <table class="table table-bordered model-list">
        <tr>
            <th style="width: 10%;">Nom</th>
            <th style="width: 10%;">Prix</th>
            <th style="width: 30%;">Description</th>
            <th style="width: 15%;">Image</th>
            <th style="width: 5%;">Actions</th>
        </tr>
        {% for model in models %}
            <tr>
                <td><a href="#" class="editable name">{{ model.name }}</a> <i class="fas fa-pen-alt min-size"></i></td>
                <td><a href="#" class="editable price">{{ model.price }} € </a><i class="fas fa-pen-alt min-size"></i></td>
                <td><a href="#" class="editable description">{{ model.description }}</a> <i class="fas fa-pen-alt min-size"></i></td>
                <td><img class="imgModeleBase" src="{{ asset('images/modeles/' ~ model.img) }}"></td>
                <td class="list-actions">
                    <button class="trash-element"><i class="fas fa-trash-alt"></i></button>
                </td>
                <input class="action-id" type="hidden" value="{{ model.id }}">
            </tr>
        {% endfor %}
    </table>
    <div class="box-footer clearfix">
        <form role="form" id="form-admin">
            <div class="row">
                <div class="col-sm">
                    <div class="form-group">
                        {{ form_row(formModel.name) }}
                    </div>
                </div>
                <div class="col-sm">
                    <div class="form-group">
                        {{ form_row(formModel.price) }}
                    </div>
                </div>
                <div class="col-sm">
                    <div class="form-group">
                        {{ form_row(formModel.description) }}
                    </div>
                </div>
{#                <div class="col-sm">#}
{#                    <div class="form-group d-flex flex-column justify-content-center">#}
{#                        <label for="uploadImg">Choisissez votre image</label>#}
{#                        <input type="file" id="uploadImg">#}
{#                    </div>#}
{#                </div>#}
                <div class="col-sm align-self-center">
                    <div class="form-group">
                        <button type="submit" class="btn bg-olive margin add-element" data-type="modeles">Ajouter</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>