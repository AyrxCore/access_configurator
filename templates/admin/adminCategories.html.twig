<div class="tab-pane" id="tab_3">
    <table class="table table-bordered category-list">
        <tr>
            <th style="width: 50%;">Nom</th>
            <th style="width: 50%;">Action</th>
        </tr>
        {% for categorie in categories %}
            <tr>
                <td><a href="#" class="editable name" data-type="text" data-pk="{{ categorie.id }}" data-url="{{ path('edit_element') }}" data-name="categories">{{ categorie.name }}</a> <i class="fas fa-pen-alt min-size"></i></td>
                <td class="list-actions">
                    <button class="trash-element" data-id="{{ categorie.id }}"><i class="fas fa-trash-alt"></i></button>
                </td>
            </tr>
        {% endfor %}
    </table>
    <div class="box-footer clearfix">
        <form role="form" id="form-admin">
            <div class="row">
                <div class="col-sm">
                    <div class="form-group">
                        {{ form_row(formCategory.name) }}
                    </div>
                </div>
                <div class="col-sm align-self-center">
                    <div class="form-group">
                        <button type="submit" class="btn bg-olive margin add-element" data-type="categories">Ajouter</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>